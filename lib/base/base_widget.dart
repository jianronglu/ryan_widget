import 'package:flutter/material.dart';

class BaseTextView extends StatelessWidget {

  const BaseTextView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
